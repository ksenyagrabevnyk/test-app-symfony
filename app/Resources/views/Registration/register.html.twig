<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>

    <link href="{{ asset('includes/admin/css/font-awesome.min.css') }}" rel="stylesheet">
    <link href="{{ asset('includes/admin/css/animate.css') }}" rel="stylesheet">
    <link href="{{ asset('includes/admin/css/bootstrap.min.css') }}" rel="stylesheet">
    <link href="{{ asset('includes/admin/inspinia/css/style.css') }}" rel="stylesheet">
    <link href="{{ asset('includes/admin/css/style-for-register-form.css') }}" rel="stylesheet">
</head>

<body>
<div class="form">

    <ul class="tab-group">
        <li class="tab active"><a href="#signup">Зарегистрироваться</a></li>
        <li class="tab-group"><a href="{{ path('login') }}">Войти в систему
        </a>
        </li>
    </ul>

    <div class="tab-content">
        <div id="signup">
            <h1>Pегистрация</h1>
            {{ form_start(form) }}
                <div class="field-wrap">
                    {{ form_row(form.firstName, {'attr': {'class': 'req'}}) }}
                </div>
                <div class="field-wrap">
                    {{ form_row(form.secondName, {'attr': {'class': 'req'}}) }}
                </div>
                <div class="field-wrap">
                    {{ form_row(form.thirdName, {'attr': {'class': 'req'}}) }}
                </div>
                <div class="top-row">
                    <div class="field-wrap">
                        {{ form_row(form.username, {'attr': {'class': 'req'}}) }}
                    </div>
                    <div class="field-wrap">
                        {{ form_row(form.email, {'attr': {'class': 'req'}}) }}
                    </div>
                </div>
                <div class="top-row">
                    <div class="field-wrap">
                        {{ form_row(form.city, {'attr': {'class': 'req'}}) }}
                    </div>
                    <div class="field-wrap">
                        {{ form_row(form.description, {'attr': {'class': 'req'}}) }}
                    </div>
                </div>
                <div class="field-wrap">
                    {{ form_row(form.password, {'attr': {'class': 'req'}}) }}
                </div>
                <div class="field-wrap">
                    <button type="submit" class="button button-block"/>Сохронить</button>
                </div>
            {{ form_end(form) }}
        </div>
    </div>

</div>
<script src='http://cdnjs.cloudflare.com/ajax/libs/jquery/2.1.3/jquery.min.js'></script>
<script src="{{ asset('includes/admin/js/index.js') }}"></script>

</body>
</html>
